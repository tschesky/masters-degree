name: warmup

# The packages we (may) depend on
dependencies:
  - base             # For built-in functions like `length`
# For assignment 0, don't add anything else

# The Warmup library
library:
  source-dirs: src
  exposed-modules:
    - Warmup
